# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'

target 'swift_budejie' do

use_frameworks!

pod "AFNetworking"
pod 'Alamofire', '~> 4.4.0'
pod "SDWebImage"
pod "SVProgressHUD"
pod "MJExtension"
pod "MJRefresh"
pod "DACircularProgress"
pod "pop"
pod 'SwiftyJSON', :git => 'https://github.com/SwiftyJSON/SwiftyJSON.git'

end
